<UserControl x:Class="DolarMoney.Comum.UcrTitulo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DolarMoney.Comum"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="/Comum/Padrao.xaml"/>
    </UserControl.Resources>
    <Grid Background="LightCyan">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="1"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="28"/>
            <ColumnDefinition Width="4"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="tbkTitulo" Grid.Row="0" Grid.Column="1" Style="{StaticResource gstyTextBlockTitulo}"
                   Text="Título"/>
        <Image x:Name="imgFechar" Grid.Row="0" Grid.Column="2" Style="{StaticResource gstyImageLink}"
               Source="/Comum/Fechar.png" MouseLeftButtonDown="imgFechar_MouseLeftButtonDown"
               Height="16" Width="16"/>
        <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4" Style="{StaticResource gstyBorder}"/>
    </Grid>
</UserControl>

    